import styled from "styled-components";

const Home = (props) => {
  return (
      <Container>
          <Content>
          </Content>
      </Container>
  );
};

const Container = styled.div`
    
`
const Content = styled.div`
    
`

export default Home;
